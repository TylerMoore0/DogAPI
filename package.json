{
  "name": "dog-daycare-api",
  "version": "1.0.0",
  "description": "REST API for a dog daycare management system. COMP 74 group project.",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun run src/server.ts",
    "db:reset": "bun run src/db/seed.ts"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
